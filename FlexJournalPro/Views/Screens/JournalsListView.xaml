<UserControl x:Class="FlexJournalPro.Views.Screens.JournalsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <materialDesign:Card Grid.Row="0" Margin="16,16,16,8" Padding="16">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding CreateNewJournalCommand}"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Додати новий" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Оновити" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </materialDesign:Card>

        <!-- Journals List -->
        <materialDesign:Card Grid.Row="1" Margin="16,8,16,16">
            <Grid>
                <DataGrid ItemsSource="{Binding Journals}"
                          SelectedItem="{Binding SelectedJournal}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Назва" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Ідентифікатор шаблону" Binding="{Binding PresetId}" Width="200"/>
                        <DataGridTextColumn Header="Дата створення" Binding="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                        <DataGridTemplateColumn Header="Команди" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.OpenJournalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                ToolTip="Відкрити">
                                            <materialDesign:PackIcon Kind="FolderOpen"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.DeleteJournalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                ToolTip="Видалити">
                                            <materialDesign:PackIcon Kind="Delete"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    <DataGrid.InputBindings>
                        <MouseBinding Gesture="LeftDoubleClick" 
                                      Command="{Binding OpenJournalCommand}"/>
                    </DataGrid.InputBindings>
                </DataGrid>

                <!-- Empty state -->
                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center"
                            Visibility="{Binding Journals.Count, Converter={StaticResource EqualToVisibilityConverter}, ConverterParameter=0}">
                    <materialDesign:PackIcon Kind="BookOpen" 
                                             Width="80" 
                                             Height="80" 
                                             HorizontalAlignment="Center"
                                             Foreground="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}"
                                             Opacity="0.3"/>
                    <TextBlock Text="Нічого не відкрито"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                               HorizontalAlignment="Center"
                               Margin="0,16,0,8"
                               Foreground="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}"/>
                    <TextBlock Text="Відприйте журнал або шо..."
                               Style="{StaticResource MaterialDesignBody2TextBlock}"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
