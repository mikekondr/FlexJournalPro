<UserControl x:Class="FlexJournalPro.Views.DynamicTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:FlexJournalPro.Converters"
             xmlns:mdconv="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <conv:ActionToBooleanConverter x:Key="ActionToBooleanConverter" Invert="True" />
        
        <!-- Стиль для заголовків колонок -->
        <Style x:Key="DynamicTableColumnHeaderStyle" 
               TargetType="{x:Type DataGridColumnHeader}"
               BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                        <ControlTemplate.Resources>
                            <Storyboard x:Key="GoToVisible">
                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SortDirectionIndicator" Storyboard.TargetProperty="Width">
                                    <EasingDoubleKeyFrame KeyTime="0:0:0.0" Value="0" />
                                    <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="8" />
                                </DoubleAnimationUsingKeyFrames>
                                <ThicknessAnimationUsingKeyFrames Storyboard.TargetName="HeaderContent" Storyboard.TargetProperty="Margin">
                                    <EasingThicknessKeyFrame KeyTime="0:0:0.0" Value="0,0,0,0" />
                                    <EasingThicknessKeyFrame KeyTime="0:0:0.2" Value="6,0,0,0" />
                                </ThicknessAnimationUsingKeyFrames>
                            </Storyboard>
                            <Storyboard x:Key="GoToHidden">
                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SortDirectionIndicator"  Storyboard.TargetProperty="Width">
                                    <EasingDoubleKeyFrame KeyTime="0:0:0.0" Value="8" />
                                    <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="0" />
                                </DoubleAnimationUsingKeyFrames>
                                <ThicknessAnimationUsingKeyFrames Storyboard.TargetName="HeaderContent" Storyboard.TargetProperty="Margin">
                                    <EasingThicknessKeyFrame KeyTime="0:0:0.0" Value="6,0,0,0" />
                                    <EasingThicknessKeyFrame KeyTime="0:0:0.2" Value="0,0,0,0" />
                                </ThicknessAnimationUsingKeyFrames>
                            </Storyboard>
                        </ControlTemplate.Resources>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter Value="{DynamicResource MaterialDesign.Brush.Foreground}" Property="Foreground" />
                            </Trigger>
                            <Trigger Property="DataGridColumnHeader.SortDirection" Value="Ascending">
                                <Setter Value="{DynamicResource MaterialDesign.Brush.Foreground}" Property="Foreground" />
                            </Trigger>
                            <Trigger Property="DataGridColumnHeader.SortDirection" Value="Descending">
                                <Setter Value="{DynamicResource MaterialDesign.Brush.Foreground}" Property="Foreground" />
                            </Trigger>
                            <Trigger SourceName="SortDirectionIndicator" Property="materialDesign:ListSortDirectionIndicator.IsNeutral" Value="False">
                                <Trigger.EnterActions>
                                    <BeginStoryboard Name="GoToVisible" Storyboard="{StaticResource GoToVisible}" />
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <RemoveStoryboard BeginStoryboardName="GoToVisible" />
                                    <BeginStoryboard Storyboard="{StaticResource GoToHidden}" />
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                        <Grid Background="Transparent">
                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                                    <materialDesign:ListSortDirectionIndicator Name="SortDirectionIndicator" Width="0" Height="8" 
                                                                               HorizontalAlignment="Left" VerticalAlignment="Center" IsTabStop="False" 
                                                                               ListSortDirection="{TemplateBinding SortDirection}" Opacity="0.45"
                                                                               Visibility="{Binding CanUserSortColumns , RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={x:Static mdconv:BooleanToVisibilityConverter.CollapsedInstance}}" />
                                    <ContentPresenter Name="HeaderContent" 
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                                      RecognizesAccessKey="True" 
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                        <ContentPresenter.Resources>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Setter Property="TextTrimming" Value="WordEllipsis" />
                                                <Setter Property="materialDesign:TextBlockAssist.AutoToolTip" Value="True" />
                                            </Style>
                                        </ContentPresenter.Resources>
                                    </ContentPresenter>
                                </Grid>
                            </Border>
                            <Thumb Name="PART_LeftHeaderGripper" HorizontalAlignment="Left" Style="{StaticResource MaterialDesignGridColumnHeaderGripper}" />
                            <Thumb Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Style="{StaticResource MaterialDesignGridColumnHeaderGripper}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для заголовків рядків з індикацією стану -->
        <Style x:Key="DynamicTableRowHeaderStyle" 
               TargetType="{x:Type DataGridRowHeader}">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridRowHeader}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <TextBlock x:Name="StateIcon"
                                          FontSize="16"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          Margin="5"
                                          Text=""
                                          Foreground="#757575"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsPlaceholder, FallbackValue=False}" Value="True">
                                <Setter TargetName="StateIcon" Property="Text" Value="⏳"/>
                                <Setter TargetName="StateIcon" Property="Foreground" Value="#9E9E9E"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsNewRowPlaceholder, FallbackValue=False}" Value="True">    
                                <Setter TargetName="StateIcon" Property="Text" Value="➕"/>
                                <Setter TargetName="StateIcon" Property="Foreground" Value="#4CAF50"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsDirty, FallbackValue=False}" Value="True">
                                <Setter TargetName="StateIcon" Property="Text" Value="✏️"/>
                                <Setter TargetName="StateIcon" Property="Foreground" Value="#FF9800"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для клітинок -->
        <Style x:Key="DynamicTableCellStyle" 
               TargetType="{x:Type DataGridCell}"
               BasedOn="{StaticResource MaterialDesignDataGridCell}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            
            <Style.Triggers>
                <!-- При виборі клітинки змінюємо фон, тільки рамка -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>

                <!-- При редагуванні прозорий фон, прибрати рамку -->
                <Trigger Property="IsEditing" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DynamicTableStyle"
               TargetType="{x:Type DataGrid}"
               BasedOn="{StaticResource MaterialDesignDataGrid}">
            <Setter Property="DataGrid.GridLinesVisibility" Value="All" />
        </Style>
               
    </UserControl.Resources>

    <DataGrid x:Name="DynamicGrid" 
                AutoGenerateColumns="False" 
                CanUserAddRows="False"
                IsReadOnly="{Binding Converter={StaticResource ActionToBooleanConverter}, ConverterParameter=EditJournal}"
                HeadersVisibility="All"
                SelectionMode="Single"
                SelectionUnit="Cell"
                  
                Style="{StaticResource DynamicTableStyle}"
                RowHeaderStyle="{StaticResource DynamicTableRowHeaderStyle}"
                  
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.CanContentScroll="True"

                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                EnableRowVirtualization="True"
                EnableColumnVirtualization="True"
              
                VirtualizingPanel.ScrollUnit="Pixel"
                ScrollViewer.IsDeferredScrollingEnabled="True">
    </DataGrid>
</UserControl>